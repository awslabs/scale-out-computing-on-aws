# Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.
# SPDX-License-Identifier: Apache-2.0

# This file is executed at the end of the bootstrap sequence
{% if context.get("/job/NodeType") == "login_node" %}
  # Add any customization you only want to run on login nodes
  log_info "No additional customization found for login_node."

{% elif context.get("/job/NodeType") == "dcv_node" %}
  # Add any customization you only want to run on dcv nodes
  log_info "No additional customization found for dcv_node."

{% elif context.get("/job/NodeType") == "compute_node" %}
  # Add any customization you only want to run on compute_node nodes
  log_info "No additional customization found for compute_node."

{% endif %}

# Code written below this line  will be executed on all machines

# Remember: Use context.get("/configuration/BaseOS") to create automation based on the different operating system you are planning to use